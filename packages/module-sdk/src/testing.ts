import type { LumenHost } from "./index.js";

export function createMockHost(overrides?: Partial<LumenHost>): LumenHost {
	throw new Error(
		"createMockHost is not implemented yet — coming in @lumen/module-sdk 0.2.0",
	);
}
